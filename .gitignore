node_modules/
dist/

# Environment / secrets
.env
.env.local
.env.*.local
.env.production
.env.development

# Private keys and certificates
*.pem
*.key
*.cert
*.p12
*.pfx
credentials.json

# Logs
*.log

# OS files
.DS_Store
Thumbs.db

# Claude / RuFlo tooling
.claude-flow/
.ruflo/
.claude/
.mcp.json
CLAUDE.md

# Obsidian vault index note. The parent 2ndBrain-mogging vault keeps a
# `motion-mcp.md` next to this submodule as a project-index note per its
# filename=folder convention — it's a vault artifact, not part of motion-mcp.
motion-mcp.md
